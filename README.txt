To install this program, run `npm install` in the frontend and backend folders.
To start the application, run `ng serve` in frontend, and `node .` in backend.
Mongodb is required in order to run the software, and it expects the mongo daemon to be listening on port 27017.
There are no bugs as far as I can tell, but there could be some I don't know about.
